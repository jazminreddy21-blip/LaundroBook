<?php
/**
 * EmailConfig.example
 *
 * This is a TEMPLATE, not the real config file, do not edit this file
 * directly and do not put a real password in it.
 *
 * TO USE THIS:
 * 1. Copy this entire file.
 * 2. Save the copy as "EmailConfig.php" in this same Config/ folder
 *    (remove ".example" from the filename).
 * 3. In your new EmailConfig.php, replace '16-digit-password' below
 *    with the real App Password, never type the real password into this .example file.
 * 4. Save. Your EmailConfig.php is already covered by .gitignore, so
 *    it will never be tracked or pushed to GitHub, even with the real
 *    password in it.

 */

return [
    'smtp_host' => 'smtp.gmail.com',
    'smtp_port' => 587,
    'smtp_username' => 'laundrobook@gmail.com',
    'smtp_password' => '16-digit-password',
    'smtp_secure' => 'tls',
    'from_email' => 'laundrobook@gmail.com',
    'from_name' => 'LaundroBook',
];